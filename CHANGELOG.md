# Changelog

本项目的所有重要变更都会记录在此文件中。
格式标准参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)。

## [Unreleased]

### 🛠️ 重构与基建 (Refactor)
* **Makefile 升级**：新增 install/uninstall 安装目标，支持 PREFIX 自定义路径，程序正式命名为 `ufcp`。

### ✨ 新特性 (Features)
* **存储介质环境感知**：新增物理存储介质检测机制（基于 Linux `sysfs` 接口）。程序在任务初始化阶段将自动判断源路径与目标路径所在的块设备类型（固态硬盘或机械硬盘），支持跨分区挂载的设备追溯。
* **命令行接口 (CLI)**：新增标准化命令行解析，支持 `-h/--help` 与 `-v/--version` 选项，参数为源文件路径与目标路径。

### 🚀 性能优化 (Performance)
* **机械硬盘 (HDD) 拷贝策略优化**：针对机械硬盘环境引入单线程拷贝后备机制。当检测到环境中包含机械硬盘时，程序会自动关闭多线程并发分块，切换为单线程顺序 I/O 模式，并将单次读写缓冲区调整为 1MB，以规避并发带来的磁头寻道开销及性能损耗。

## [v1.1.0] - 2026-05-03

### 🚀 性能优化 (Performance)
* **动态分块计算**：重构了分块数量的算法，摒弃硬编码。现支持基于目标文件大小（32MB阈值）与主机 CPU 逻辑核心数的自适应并发，消除了极小文件场景下的线程调度损耗，并在大文件场景下更科学地压榨硬件性能。
* **I/O 吞吐优化**：将单次读写缓冲区从默认的 4KB 提升至 4MB（现代固态存储经验甜点值）。大幅减少了 `read/write` 系统调用引发的内核态上下文切换开销，并为未来基于 OS API 动态感知底层块设备最优 I/O 大小预留了演进空间。

### 🐛 修复 (Fixed)
* **目录自动创建**：修复了目标路径所在目录不存在时导致程序直接崩溃的问题，现已支持底层级联目录 (`mkdir -p`) 的自动创建与并发安全防范。

### 🛠️ 重构与基建 (Refactor)
* **工业级日志系统 (Logger Overhaul)**：
  * **线程安全保障**：引入底层 `std::mutex` 与 RAII 锁 (`std::lock_guard`)，解决多线程满载并发时导致的日志文本交错和输出撕裂问题。
  * **目录自适应**：集成 `<filesystem>` 库，支持日志目标路径所在多级目录的自动探测与级联创建，避免因无权限或目录缺失导致的静默失败。
  * **日志分级过滤**：引入 `DEBUG`, `INFO`, `WARNING`, `ERROR` 四档级别。默认屏蔽高频调试输出，确保终端信息清爽。
  * **规范对齐**：全面重构接口命名（大驼峰）与私有成员规范，严格对齐 C++ 核心工程规范。

### 📝 工程规范 (Documentation)
* **架构演进**：新增 `docs/Architecture.md`，正式确立 V1.0.0 的“静态多线程分块”核心架构与技术背景。
* **提交模板**：引入 `.gitmessage` 规范，强制在每次底层优化时复盘问题背景、决策依据与优缺点分析。

---

## [v1.0.0] - 2026-01-26

### 🎉 初始版本
* 实现了 `UltraFastCopy` 的基本功能。